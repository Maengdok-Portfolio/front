version: '3.8'
services:
  app:
    container_name: react
    build: .
    ports:
      - '3001:3000'
    environment:
      - CHOKIDAR_USEPOLLING=true
    stdin_open: true
    tty: true
    volumes:
      - .:/app
      - node_modules:/app/node_modules
volumes:
  node_modules: